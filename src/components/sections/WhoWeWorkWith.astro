---
const audiences = [
  'SaaS teams at scale',
  'Fintech & regulated',
  'CI/CD-heavy teams',
  'AI/ML on GPUs',
  'No in-house DevOps',
];
---

<section id="who-we-work-with" class="py-12 relative">
  <div class="container-custom">
    <div class="max-w-4xl mx-auto text-center reveal">
      <!-- Tiny heading -->
      <p class="text-cyan text-xs font-semibold tracking-wide uppercase mb-2">Who we work with</p>

      <!-- Short copy -->
      <p class="text-steel text-sm mb-5">
        Engineering teams that ship fast and can't afford fragile platforms.
      </p>

      <!-- Chip row -->
      <div class="flex flex-wrap items-center justify-center gap-2">
        {audiences.map((audience) => (
          <span class="inline-flex items-center px-3 py-1.5 rounded-full text-xs font-medium border border-white/10 bg-white/5 text-steel hover:border-cyan/30 hover:text-white transition-colors">
            {audience}
          </span>
        ))}
      </div>
    </div>
  </div>
</section>
