---
// TrustedBy section - Customer reference logos
---

<section id="trusted-by" class="section-padding bg-midnight relative overflow-hidden">
  <div class="container-custom">
    <!-- Section Header -->
    <div class="text-center mb-16 reveal">
      <h2 class="text-3xl md:text-4xl font-bold mb-4">
        <span class="text-gradient">Trusted Partners</span>
      </h2>
      <p class="text-steel text-lg max-w-2xl mx-auto">
        We're proud to partner with innovative companies who trust us with their technology needs
      </p>
    </div>

    <!-- Logo Grid -->
    <div class="reveal">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-12 md:gap-16 items-center justify-items-center">
        <!-- Callflows -->
        <div class="logo-container">
          <img
            src="/logos/callflows_brand_no_claim.webp"
            alt="Callflows"
            class="customer-logo"
          />
        </div>

        <!-- Grothastic -->
        <div class="logo-container">
          <img
            src="/logos/Logo-Color.svg"
            alt="Grothastic"
            class="customer-logo"
          />
        </div>

        <!-- techgardens -->
        <div class="logo-container">
          <img
            src="/logos/techgardens.png"
            alt="techgardens"
            class="customer-logo logo-white"
          />
        </div>

        <!-- theiafiber -->
        <div class="logo-container">
          <img
            src="/logos/theiafiber.png"
            alt="theiafiber"
            class="customer-logo logo-white logo-theiafiber"
          />
        </div>
      </div>
    </div>
  </div>

  <!-- Decorative gradient -->
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] bg-electric/5 rounded-full blur-3xl -z-10"></div>
</section>

<style>
  .logo-container {
    width: 100%;
    max-width: 220px;
    height: 100px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1rem;
  }

  .customer-logo {
    max-width: 100%;
    max-height: 100%;
    width: auto;
    height: auto;
    object-fit: contain;
    /* Convert logos to uniform greyscale */
    filter: grayscale(1) brightness(1.2) contrast(0.8) opacity(0.7);
    transition: all 0.3s ease;
  }

  .customer-logo:hover {
    filter: grayscale(1) brightness(1.4) contrast(0.9) opacity(1);
    transform: scale(1.05);
  }

  /* White logos for techgardens and theiafiber */
  .logo-white {
    filter: brightness(0) invert(1) opacity(0.8) !important;
  }

  .logo-white:hover {
    filter: brightness(0) invert(1) opacity(1) !important;
    transform: scale(1.05);
  }

  /* Align theiafiber logo vertically */
  .logo-theiafiber {
    transform: translateY(-8px);
  }

  .logo-theiafiber:hover {
    transform: translateY(-8px) scale(1.05) !important;
  }

  @media (max-width: 768px) {
    .logo-container {
      max-width: 160px;
      height: 80px;
    }

    .logo-theiafiber {
      transform: translateY(-6px);
    }

    .logo-theiafiber:hover {
      transform: translateY(-6px) scale(1.05) !important;
    }
  }
</style>
