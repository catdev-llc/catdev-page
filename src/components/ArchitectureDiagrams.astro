---
// Compact 4-pillar strip - sits directly above the offer cards as a light "legend"
const pillars = [
  {
    label: 'Infrastructure',
    description: 'Terraform, Ansible, multi-cloud networking, secrets and baseline security.',
  },
  {
    label: 'Delivery pipeline',
    description: 'GitHub / GitLab CI or Azure DevOps—build, test, scan and deploy without friction.',
  },
  {
    label: 'Supply-chain security',
    description: 'SBOM + VEX, Sigstore signing and SLSA-aligned provenance for every artifact.',
  },
  {
    label: 'Kubernetes runtime',
    description: 'GitOps, policies, observability and DR for production clusters.',
  },
];
---

<!-- Compact 4-Pillar Strip -->
<div class="rounded-2xl border border-white/10 bg-deep-space/40 p-5 sm:p-6">
  <div class="grid gap-5 md:grid-cols-4 md:gap-4">
    {pillars.map((pillar) => (
      <div>
        <p class="text-xs font-semibold text-cyan uppercase tracking-wide mb-1.5">
          {pillar.label}
        </p>
        <p class="text-sm text-steel leading-relaxed">
          {pillar.description}
        </p>
      </div>
    ))}
  </div>
</div>
